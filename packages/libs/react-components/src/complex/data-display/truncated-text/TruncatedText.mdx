# TruncatedText

<State initialState={{maxLines: 0}}>

  <Observe>
    {({setState}) => (
      <button onClick={() => setState(s => ({maxLines: maxLines.count + 1}))}>
        Click me
      </button>
    )}
  </Observe>

  <Observe>
    {({maxLines}) =>
      <Box px={2} py={1} border={"solid 2px"} borderColor={"border.default"} width={200}>
        <TruncatedText as={"p"} maxLines={maxLines}>
          this text is truncated by line count.
        </TruncatedText>
      </Box>
    }
  </Observe>

</State>
