<!--- TruncatedText.stories.mdx -->

import {Canvas, Meta, Story} from "@storybook/addon-docs/blocks"
import {TruncatedText} from "./TruncatedText"

<Meta title="Components/Truncated Text" component={TruncatedText} />

# Truncated Text

##

A component used to truncate text with an ellipsis.

<!--- This is your Story template function, shown here in React -->

export const Template = (args) => <TruncatedText maxLines={2} width={300} {...args} />

<Canvas>
  <Story name="Default" args={{
    children: "Hello",
    primary: true,
    label: 'Default'
  }}>
    {Template.bind({})}
  </Story>
</Canvas>
